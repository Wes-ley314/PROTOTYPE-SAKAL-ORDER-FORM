/* ══════════════════════════════════════════════════
   SETUP
   Connecting the sheet, and putting the app on the
   home screen. Both live inside the app so there is
   no file to open on a phone.
   ══════════════════════════════════════════════════ */

var APPS_SCRIPT_CODE = "/**\n * \u0160AKAL Workshop \u2014 storage for everything that is not an order,\n * plus readable Invoicing and Delta tabs.\n *\n * Orders already save to your sheet. This adds the same for invoices,\n * customers, the price list, stock, leads, operation cards, measurements\n * and Delta, so nothing lives on one phone only.\n *\n * It also takes the order-form PDF the shop attaches to an order, files\n * it in a Drive folder next to this sheet, and hands the link back.\n *\n * Two things are written on every save:\n *   1. The exact data, as JSON, into a hidden \"Modules\" sheet. This is\n *      what the app reads back, and it is the only copy that survives a\n *      round trip without losing nested detail (suit order forms, the\n *      garments inside an invoice line, the add-ons on a Delta order).\n *   2. A readable mirror, into the \"Invoicing\" and \"Delta\" tabs, for\n *      anyone reading the sheet with their eyes.\n *\n * The mirror tabs are rewritten in full on every save. Do not type into\n * them \u2014 anything you add by hand is wiped the next time the app saves.\n * Put your own working columns on a separate tab and point formulas at\n * these.\n *\n * HOW TO ADD IT\n *  1. Open the Apps Script project behind your \u0160AKAL sheet.\n *  2. Replace the old module block with everything below (or paste it at\n *     the bottom if this is the first time).\n *  3. Find your existing doPost(e) and add the marked lines at the very\n *     top of it (see HOOK 1).\n *  4. Find your existing doGet(e) and add the marked line at the very\n *     top of it (see HOOK 2).\n *  5. Deploy \u2192 Manage deployments \u2192 edit the live deployment \u2192\n *     Version: New version \u2192 Deploy. The URL stays the same.\n *  6. Run rebuildMirrors once from the editor to fill the Invoicing and\n *     Delta tabs from what is already stored.\n *  7. In the app, press \"Save all to sheet\" in the bottom left once.\n *\n * HOOK 1 \u2014 first lines inside your existing doPost(e):\n *\n *     var body = JSON.parse(e.postData.contents);\n *     if (body.action === 'SAVE_MODULE') return saveModule_(body);\n *     if (body.action === 'UPLOAD_PDF') return uploadPdf_(body);\n *     if (body.action === 'DELETE_PDF') return deletePdf_(body);\n *     if (body.action === 'ASK_AI') return askAi_(body);\n *     // ...your existing order handling carries on below, using body\n *\n * HOOK 2 \u2014 first lines inside your existing doGet(e):\n *\n *     if (e && e.parameter && e.parameter.modules) return getModules_();\n *     if (e && e.parameter && e.parameter.ai) return aiReady_();\n *     // ...your existing order handling carries on below\n */\n\nvar MODULE_SHEET = 'Modules';\nvar CHUNK = 40000;            // a cell holds 50,000 characters; leave room\n\n/* 'delta' is the new one. Without it every Delta save came back\n   \"Unknown module: delta\" and the app quietly fell back to the device. */\nvar MODULE_NAMES = ['measurements', 'invoices', 'pricelist', 'stock',\n                    'leads', 'customers', 'ops', 'delta', 'attachments'];\n\n/* Where the order-form PDFs land, and who may open one.\n   Link sharing is on so a phone on the shop floor opens the file without\n   signing in to this Google account. Set it to false to keep the files\n   private, and everyone who needs one will have to be given access. */\nvar PDF_FOLDER = '\\u0160AKAL order forms';\nvar PDF_LINK_SHARING = true;\n\n/* Which lists also get written out as readable rows, and into which tab.\n   Add a line here plus a builder function to mirror another list. */\nvar MIRRORS = {\n  invoices: {sheet: 'Invoicing', build: invoiceRows_},\n  delta:    {sheet: 'Delta',     build: deltaRows_}\n};\n\nfunction moduleSheet_() {\n  var ss = SpreadsheetApp.getActiveSpreadsheet();\n  var sh = ss.getSheetByName(MODULE_SHEET);\n  if (!sh) {\n    sh = ss.insertSheet(MODULE_SHEET);\n    sh.appendRow(['module', 'part', 'data', 'updated']);\n    sh.hideSheet();\n  }\n  return sh;\n}\n\nfunction jsonOut_(obj) {\n  return ContentService\n    .createTextOutput(JSON.stringify(obj))\n    .setMimeType(ContentService.MimeType.JSON);\n}\n\n/** Stores one list. Rewrites only that list's rows. */\nfunction saveModule_(body) {\n  var lock = LockService.getScriptLock();\n  try {\n    lock.waitLock(20000);\n\n    var name = String(body.module || '').trim();\n    if (MODULE_NAMES.indexOf(name) === -1) {\n      return jsonOut_({status: 'error', message: 'Unknown module: ' + name});\n    }\n\n    var sh = moduleSheet_();\n    var text = JSON.stringify(body.payload === undefined ? null : body.payload);\n    var stamp = new Date().toISOString();\n\n    // Drop this module's existing rows, bottom up so the indexes hold.\n    var last = sh.getLastRow();\n    if (last > 1) {\n      var col = sh.getRange(2, 1, last - 1, 1).getValues();\n      for (var r = col.length - 1; r >= 0; r--) {\n        if (String(col[r][0]) === name) sh.deleteRow(r + 2);\n      }\n    }\n\n    // Write it back in chunks a cell can hold.\n    var rows = [];\n    for (var i = 0, part = 0; i < text.length; i += CHUNK, part++) {\n      rows.push([name, part, text.substring(i, i + CHUNK), stamp]);\n    }\n    if (!rows.length) rows.push([name, 0, '', stamp]);\n    sh.getRange(sh.getLastRow() + 1, 1, rows.length, 4).setValues(rows);\n\n    // The readable copy. A failure here must never cost us the save,\n    // so it is caught and reported rather than thrown.\n    var mirrored = '';\n    try {\n      mirrored = writeMirror_(name, body.payload);\n    } catch (e) {\n      mirrored = 'failed: ' + String(e);\n    }\n\n    return jsonOut_({status: 'success', module: name, parts: rows.length,\n                     updated: stamp, mirrored: mirrored});\n  } catch (err) {\n    return jsonOut_({status: 'error', message: String(err)});\n  } finally {\n    try { lock.releaseLock(); } catch (e) {}\n  }\n}\n\n/** Reads one stored list back, without dragging the rest along. */\nfunction readModule_(name) {\n  try {\n    var sh = moduleSheet_(), last = sh.getLastRow();\n    if (last < 2) return null;\n    var parts = [];\n    sh.getRange(2, 1, last - 1, 3).getValues().forEach(function (row) {\n      if (String(row[0]) !== name) return;\n      parts.push({part: Number(row[1]) || 0, data: String(row[2] == null ? '' : row[2])});\n    });\n    if (!parts.length) return null;\n    parts.sort(function (a, b) { return a.part - b.part; });\n    return JSON.parse(parts.map(function (c) { return c.data; }).join(''));\n  } catch (e) {\n    return null;\n  }\n}\n\n/** Hands every stored list back in one response. */\nfunction getModules_() {\n  try {\n    var sh = moduleSheet_();\n    var out = {};\n    var last = sh.getLastRow();\n\n    if (last > 1) {\n      var vals = sh.getRange(2, 1, last - 1, 3).getValues();\n      var buckets = {};\n      vals.forEach(function (row) {\n        var name = String(row[0]);\n        if (!name) return;\n        if (!buckets[name]) buckets[name] = [];\n        buckets[name].push({part: Number(row[1]) || 0, data: String(row[2] == null ? '' : row[2])});\n      });\n      Object.keys(buckets).forEach(function (name) {\n        buckets[name].sort(function (a, b) { return a.part - b.part; });\n        var text = buckets[name].map(function (c) { return c.data; }).join('');\n        if (!text) return;\n        try { out[name] = JSON.parse(text); } catch (e) { /* leave it out rather than send rubbish */ }\n      });\n    }\n\n    return jsonOut_({status: 'success', modules: out});\n  } catch (err) {\n    return jsonOut_({status: 'error', message: String(err)});\n  }\n}\n\n\n/* \u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\n   THE ORDER FORM PDFs\n   One file per order. The app sends it as text; this turns it back into\n   a PDF, files it, and answers with the link the order keeps.\n   \u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550 */\n\nfunction pdfFolder_() {\n  var it = DriveApp.getFoldersByName(PDF_FOLDER);\n  return it.hasNext() ? it.next() : DriveApp.createFolder(PDF_FOLDER);\n}\n\n/** Takes one PDF and files it under the order number. */\nfunction uploadPdf_(body) {\n  try {\n    var num  = String(body.orderNum || '').trim();\n    var data = String(body.data || '');\n    if (!num)  return jsonOut_({status: 'error', message: 'No order number on the upload.'});\n    if (!data) return jsonOut_({status: 'error', message: 'No file on the upload.'});\n\n    var given = String(body.name || '').replace(/[\\\\/:*?\"<>|]/g, ' ').trim();\n    if (given.slice(-4).toLowerCase() !== '.pdf') given += '.pdf';\n    var prefix = num + ' \\u2014 ';\n    var name = prefix + given;\n\n    var folder = pdfFolder_();\n\n    /* One PDF per order: anything already filed under this number goes to\n       the bin, so a re-attach never leaves two versions side by side. */\n    var files = folder.getFiles();\n    while (files.hasNext()) {\n      var f = files.next();\n      if (f.getName().indexOf(prefix) === 0) {\n        try { f.setTrashed(true); } catch (e) {}\n      }\n    }\n\n    var blob = Utilities.newBlob(Utilities.base64Decode(data), 'application/pdf', name);\n    var file = folder.createFile(blob);\n    if (PDF_LINK_SHARING) {\n      try {\n        file.setSharing(DriveApp.Access.ANYONE_WITH_LINK, DriveApp.Permission.VIEW);\n      } catch (e) { /* a domain that forbids link sharing keeps the file private */ }\n    }\n\n    return jsonOut_({status: 'success', file: {\n      id: file.getId(),\n      name: name,\n      url: 'https://drive.google.com/file/d/' + file.getId() + '/view',\n      size: file.getSize(),\n      updated: new Date().toISOString()\n    }});\n  } catch (err) {\n    return jsonOut_({status: 'error', message: String(err)});\n  }\n}\n\n/** Bins one PDF. The file stays in the Drive bin for 30 days. */\nfunction deletePdf_(body) {\n  try {\n    var id = String(body.fileId || '').trim();\n    if (!id) return jsonOut_({status: 'error', message: 'No file id on the request.'});\n    DriveApp.getFileById(id).setTrashed(true);\n    return jsonOut_({status: 'success', id: id});\n  } catch (err) {\n    return jsonOut_({status: 'error', message: String(err)});\n  }\n}\n\n\n/* \u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\n   THE READABLE TABS\n   One-way: the app writes, the sheet shows. Reading back is always\n   from the JSON above, because a flat grid cannot carry a line's\n   garments home again in one piece.\n   \u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550 */\n\nfunction writeMirror_(name, payload) {\n  var m = MIRRORS[name];\n  if (!m) return '';\n\n  var rows = m.build(payload || []);\n  var ss = SpreadsheetApp.getActiveSpreadsheet();\n  var sh = ss.getSheetByName(m.sheet);\n  var fresh = false;\n  if (!sh) { sh = ss.insertSheet(m.sheet); fresh = true; }\n\n  sh.clearContents();\n  if (!rows.length) return m.sheet + ' (empty)';\n\n  var width = rows[0].length;\n  sh.getRange(1, 1, rows.length, width).setValues(rows);\n  sh.setFrozenRows(1);\n  sh.getRange(1, 1, 1, width).setFontWeight('bold');\n  if (fresh) sh.autoResizeColumns(1, width);\n\n  return m.sheet + ' (' + (rows.length - 1) + ')';\n}\n\nfunction pad_(row, width) {\n  while (row.length < width) row.push('');\n  return row;\n}\n\nfunction yn_(v) { return v ? 'Yes' : 'No'; }\n\n\n/* \u2500\u2500 Invoicing \u2500\u2500\n   One row per garment, so fabric and lining have somewhere to sit.\n   The identity columns repeat down every row so filters work, but the\n   invoice-level money lands on the first row of each invoice only \u2014\n   otherwise summing the Total column would count a three-garment\n   invoice three times. SUM(Line total) and SUM(Invoice total) both\n   give you a true figure. */\nvar INVOICE_HEAD = [\n  'Invoice no.', 'Status', 'Issued', 'Due', 'Fitting',\n  'Client', 'Phone', 'Email', 'Staff', 'Order no.',\n  'Line item', 'Garment', 'Fabric', 'Lining', 'Order form PDF',\n  'Qty', 'Unit price', 'Line total',\n  'Subtotal', 'Discount', 'Invoice total', 'Paid', 'Balance',\n  'Note', 'Updated'\n];\n\nfunction invoiceRows_(list) {\n  var out = [INVOICE_HEAD.slice()];\n  var pdfs = readModule_('attachments') || {};\n\n  (list || []).forEach(function (v) {\n    var lines = v.lines || [];\n\n    var sub = 0;\n    lines.forEach(function (l) { sub += (Number(l.qty) || 0) * (Number(l.unit) || 0); });\n    var disc  = Number(v.discount) || 0;\n    var total = Math.max(0, sub - disc);\n    var paid  = Number(v.paid) || 0;\n\n    // Same three states the app shows on the invoice list.\n    var status = (paid >= total - 0.005 && (total > 0 || v.sent))\n      ? 'Paid'\n      : (v.sent ? 'Sent' : 'Draft');\n\n    var head = [v.no || '', status, v.issued || '', v.due || '', v.fitting || '',\n                v.client || '', v.phone || '', v.email || '', v.staff || '', v.orderNum || ''];\n\n    var body = [];\n\n    lines.forEach(function (l) {\n      var qty = Number(l.qty) || 0, unit = Number(l.unit) || 0;\n      // A \"2-piece suit\" line carries a part per garment; a plain line does not.\n      var parts = (l.parts && l.parts.length) ? l.parts : [null];\n\n      parts.forEach(function (p, pi) {\n        body.push(head.concat([\n          l.desc || '',\n          p ? (p.product || '') : (l.garments || []).join(' \u00b7 '),\n          p ? (p.fabric || '') : (l.fabric || ''),\n          p ? (p.lining || '') : (l.lining || ''),\n          (pdfs[v.orderNum] && pdfs[v.orderNum].url) ? pdfs[v.orderNum].url : '',\n          pi === 0 ? qty : '',\n          pi === 0 ? unit : '',\n          pi === 0 ? qty * unit : '',\n          '', '', '', '', '',          // invoice money \u2014 first row only, filled below\n          v.note || '', v.updated || ''\n        ]));\n      });\n    });\n\n    // An invoice with no line items still deserves a row.\n    if (!body.length) {\n      body.push(head.concat(['', '', '', '', '', '', '', '',\n                             '', '', '', '', '',\n                             v.note || '', v.updated || '']));\n    }\n\n    body[0][18] = sub;\n    body[0][19] = disc;\n    body[0][20] = total;\n    body[0][21] = paid;\n    body[0][22] = total - paid;\n\n    body.forEach(function (r) { out.push(pad_(r, INVOICE_HEAD.length)); });\n  });\n\n  return out;\n}\n\n\n/* \u2500\u2500 Delta \u2500\u2500\n   One row per Delta order. The charge is whatever the app worked out and\n   saved, so the price of a canvas or an add-on stays defined in one place\n   and this never drifts from it. */\nvar DELTA_HEAD = [\n  'Delta no.', 'Placed', 'Due', 'Canvas', 'Add-ons', 'Charge (S$)', 'Stage',\n  'Back-End', 'Bin', 'Delivered', 'Paid', 'Invoiced', 'Archived'\n];\n\nfunction deltaRows_(list) {\n  var out = [DELTA_HEAD.slice()];\n\n  (list || []).forEach(function (o) {\n    // Read in the same order the app reads it, so the two never disagree.\n    var stage = o.backend ? 'Back-End'\n              : (o.bin ? 'Bin'\n              : (o.delivered ? 'Delivered' : 'Not started'));\n\n    var charge = (o.total === undefined || o.total === null || o.total === '')\n      ? '' : Number(o.total);\n\n    out.push(pad_([\n      o.orderNum || '', o.date || '', o.dueDate || '', o.canvas || '',\n      (o.addons || []).join(' \u00b7 '),\n      charge,\n      stage,\n      yn_(o.backend), yn_(o.bin), yn_(o.delivered),\n      yn_(o.paid), yn_(o.invoice), yn_(o.archived)\n    ], DELTA_HEAD.length));\n  });\n\n  return out;\n}\n\n\n/** Run once from the editor to fill the readable tabs from what is\n *  already stored, without touching the app. */\nfunction rebuildMirrors() {\n  var res = JSON.parse(getModules_().getContent());\n  var mods = res.modules || {};\n  Object.keys(MIRRORS).forEach(function (name) {\n    if (mods[name]) Logger.log(writeMirror_(name, mods[name]));\n    else Logger.log(name + ': nothing stored yet');\n  });\n}\n\n/** Run once from the editor to check the sheet and permissions are fine. */\nfunction testModules() {\n  saveModule_({module: 'delta', payload: [{orderNum: '00000', dueDate: '2026-01-01',\n    canvas: 'Half Canvas', addons: ['Surgeon'], total: 150, date: '1 Jan 2026',\n    backend: true, bin: false, delivered: false, paid: false, invoice: false, archived: false}]});\n  Logger.log(getModules_().getContent());\n}\n\n\n/* \u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\n   THE ASSISTANT\n   Lets the app ask Anthropic a question without the key ever\n   leaving Google. The hooks are listed at the top of this file.\n\n   Put the key in: Project Settings \u2192 Script Properties \u2192\n   Add script property \u2192 name ANTHROPIC_KEY, value sk-ant-\u2026\n   \u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550 */\n\nfunction askAi_(body) {\n  try {\n    var key = PropertiesService.getScriptProperties().getProperty('ANTHROPIC_KEY');\n    if (!key) {\n      return jsonOut_({status: 'error',\n        message: 'No ANTHROPIC_KEY in Script Properties yet.'});\n    }\n    var res = UrlFetchApp.fetch('https://api.anthropic.com/v1/messages', {\n      method: 'post',\n      contentType: 'application/json',\n      muteHttpExceptions: true,\n      headers: {'x-api-key': key, 'anthropic-version': '2023-06-01'},\n      payload: JSON.stringify(body.payload || {})\n    });\n    var data = JSON.parse(res.getContentText());\n    if (data.error) return jsonOut_({status: 'error', message: data.error.message});\n    var reply = (data.content || [])\n      .filter(function (b) { return b.type === 'text'; })\n      .map(function (b) { return b.text; })\n      .join('\\n');\n    return jsonOut_({status: 'success', reply: reply});\n  } catch (err) {\n    return jsonOut_({status: 'error', message: String(err)});\n  }\n}\n\n/** Lets the app check whether an assistant is set up before it asks anything. */\nfunction aiReady_() {\n  var key = PropertiesService.getScriptProperties().getProperty('ANTHROPIC_KEY');\n  return jsonOut_({status: 'success', ai: !!key});\n}\n";

function openSetup() {
  $('setup-code').textContent = APPS_SCRIPT_CODE;
  $('setup-state').textContent = modulesOnServer === true
    ? 'The sheet is answering — your lists are saving there.'
    : (modulesOnServer === false
        ? 'The sheet is not taking the lists yet, so they are on this device only. The five steps below fix that.'
        : 'Checking the sheet…');
  $('setup-modal').classList.add('open');
  if (modulesOnServer === null) checkSheetLink(true);
}
function closeSetup() { $('setup-modal').classList.remove('open'); }

function copySetupCode() {
  var text = APPS_SCRIPT_CODE;
  function fallback() {
    var ta = document.createElement('textarea');
    ta.value = text;
    ta.style.position = 'fixed'; ta.style.opacity = '0';
    document.body.appendChild(ta);
    ta.focus(); ta.select(); ta.setSelectionRange(0, text.length);
    var ok = false;
    try { ok = document.execCommand('copy'); } catch(e) {}
    document.body.removeChild(ta);
    showToast(ok ? 'Code copied — paste it into Apps Script'
                 : 'Select the code and copy it by hand');
  }
  if (navigator.clipboard && navigator.clipboard.writeText) {
    navigator.clipboard.writeText(text)
      .then(function(){ showToast('Code copied — paste it into Apps Script'); })
      .catch(fallback);
  } else { fallback(); }
}

async function checkSheetLink(quiet) {
  $('setup-state').textContent = 'Checking the sheet…';
  var ok = await fetchModulesFromServer();
  var pdfOk = await pdfBackendReady(true);   // asked fresh, not from earlier in the session

  $('setup-state').textContent = (ok
    ? 'The sheet is answering — your lists are saving there.'
    : 'The sheet is not taking the lists yet, so they are on this device only. The five steps below fix that.')
    + (pdfOk
        ? ' Order-form PDFs are on.'
        : ' Order-form PDFs are NOT on — the script is not on V9 yet, or Drive has not been allowed.');

  if (!quiet) showToast(ok ? (pdfOk ? 'Connected' : 'Connected — but no PDFs yet') : 'Not connected yet');
  setSync(ok ? '' : 'warn', ok ? 'All changes saved' : 'Orders on the sheet · lists on this device');
}

/* ── Home screen ── */
function platformGuess() {
  var ua = navigator.userAgent || '';
  var touchMac = navigator.maxTouchPoints > 1 && /Macintosh/.test(ua);
  if (/iPad/.test(ua) || touchMac) return 'ipad';
  if (/iPhone|iPod/.test(ua)) return 'iphone';
  if (/Macintosh/.test(ua)) return 'mac';
  return 'other';
}
function openInstall() {
  var p = platformGuess();
  var standalone = window.matchMedia && window.matchMedia('(display-mode: standalone)').matches
                || window.navigator.standalone === true;
  var local = location.protocol === 'file:';

  var steps = {
    iphone: [
      'Open this page in <b>Safari</b>.',
      'Tap the <b>Share</b> button at the bottom — the square with an arrow.',
      'Scroll down and tap <b>Add to Home Screen</b>.',
      'Name it ŠAKAL and tap <b>Add</b>. It opens full screen, with no address bar.'
    ],
    ipad: [
      'Open this page in <b>Safari</b>.',
      'Tap the <b>Share</b> button in the top toolbar.',
      'Tap <b>Add to Home Screen</b>.',
      'Name it ŠAKAL and tap <b>Add</b>. It runs in its own window, and works in Split View.'
    ],
    mac: [
      'Open this page in <b>Safari</b>.',
      'Choose <b>File → Add to Dock</b> (Safari 17 or newer). In Chrome it is the install icon in the address bar.',
      'Name it ŠAKAL and click <b>Add</b>.',
      'It now opens from the Dock like any other Mac app.'
    ],
    other: [
      'Open this page in Chrome or Edge.',
      'Use the install icon in the address bar, or the menu → <b>Install</b>.',
      'It then opens in its own window.'
    ]
  }[p];

  $('install-msg').innerHTML = standalone
    ? 'You are already running it as an app. Nothing more to do.'
    : (local
        ? 'This copy is open straight from a file, so the home screen cannot hold it. Put the file on a web address first — any static host will do — then follow these steps on that address.'
        : 'A few taps and ŠAKAL sits on the home screen like any other app, full screen and offline-ready.');
  $('install-steps').innerHTML = standalone ? ''
    : steps.map(function(s, i) {
        return '<div class="step"><span class="step-n">'+(i+1)+'</span><span class="step-t">'+s+'</span></div>';
      }).join('')
      + '<div class="inline-note" style="margin-top:12px">Everything is saved to your Google Sheet, so the same orders show up on the shop iPad, your iPhone and the Mac.</div>';

  $('install-modal').classList.add('open');
}
function closeInstall() { $('install-modal').classList.remove('open'); }